/**
 * Public surface for generated GraphQL types and documents. Re-exports both
 * the client-preset `graphql()` helper and every typed document/type, so
 * consumers import from one place:
 *
 *   import { ViewerDocument, type ViewerQuery, graphql } from "@drivebase/data/gql"
 */
export * from "./__generated__/gql"
export * from "./__generated__/graphql"
