{
	"name": "drivebase",
	"version": "2.1.4",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"api": "bun --env-file=.env.local turbo run --filter=api dev",
		"generate": "bun --filter web generate && bun --filter api codegen",
		"build": "bun --env-file=.env turbo run build --filter=api --filter=web",
		"dev": "bun --env-file=.env.local turbo run dev --parallel --filter=api --filter=web",
		"dev:local": "bun --env-file=.env.local turbo run dev --parallel --filter=api --filter=web",
		"test": "bun test --env-file=.env.test",
		"test:watch": "bun test --env-file=.env.test --watch",
		"check-types": "turbo run check-types",
		"db:generate": "bun run --filter=db generate",
		"db:migrate": "bun run --filter=db migrate",
		"db:studio": "bun run --filter=db studio",
		"release": "release-it",
		"prepare": "husky",
		"format": "biome format --write",
		"check": "biome check --write"
	},
	"overrides": {
		"yaml": "2.7.0"
	},
	"resolutions": {
		"yaml": "2.7.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.14",
		"@commitlint/cli": "^20.4.1",
		"@commitlint/config-conventional": "^20.4.1",
		"@release-it/bumper": "^7.0.5",
		"@release-it/conventional-changelog": "^10.0.5",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"release-it": "^19.2.4",
		"turbo": "^2.8.10",
		"yaml": "2.7.0"
	},
	"packageManager": "bun@1.3.5"
}
